server.port=8080

# Configura��es de conex�o PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/secretaria
spring.datasource.username=postgres
spring.datasource.password=postgres

# Mostra as queries no console
spring.jpa.show-sql=true

# Permite que o Hibernate atualize as tabelas (crie se n�o existir, altere se necess�rio)
spring.jpa.hibernate.ddl-auto=update

# Dialeto PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

logging.level.org.springframework.security=DEBUG
